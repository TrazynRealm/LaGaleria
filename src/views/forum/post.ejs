<%- include("../templates/head") %>

<%- include("../templates/header") %>

<div class="container mx-auto py-8">
    <!-- Contenido del post -->
    <div class="mb-8">
        <h1 class="text-3xl font-semibold mb-4"><%= post.title %></h1>
        <p class="mb-4"><%= post.content %></p>
        <p class="text-gray-600">Por <%= post.author.username %>, <%= post.createdAt %></p>
    </div>

    <!-- Comentarios -->
    <div>
        <h2 class="text-xl font-semibold mb-2">Comentarios</h2>
        <% if (comments.length === 0) { %>
            <p>No hay comentarios aún.</p>
        <% } else { %>
            <% comments.forEach(comment => { %>
                <div class="mb-4">
                    <p class="text-gray-600">Por <%= comment.author.username %>, <%= comment.createdAt %></p>
                    <p><%= comment.content %></p>
                </div>
            <% }); %>
        <% } %>
    </div>
</div>

<!-- Aquí va el HTML del pie de página, como los enlaces de redes sociales, información de contacto, etc. -->
