<%- include("../templates/header") %>

<div class="container mx-auto py-6">
    <div class="max-w-4xl mx-auto">
        <h2 class="text-3xl font-semibold mb-4"><%= product.name %></h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            <div>
                <img src="https://via.placeholder.com/300x200" alt="<%= product.name %>" class="rounded-lg">
            </div>
            <div>
                <p class="text-gray-600"><%= product.description %></p>
                <p class="text-xl font-semibold mt-4">$<%= product.price %></p>
                <button class="bg-blue-500 text-white py-2 px-4 mt-4 rounded-lg">Añadir al carrito</button>
            </div>
        </div>
    </div>
</div>
